stages:
  - setup
  - build
  - deploy

cache:
  paths:
  - node_modules/

job:setup:
  stage: setup
  script:
    - yarn ci

job:build:
  stage: build
  script:
    - yarn build
  artifacts:
    paths:
    - dist/
    expire_in: 1 week

job:deploy:
  stage: deploy
  script:
    - netlify deploy -b $CI_COMMIT_REF_SLUG --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN
  except:
    - master